[project]
name = "walkpick-search"
version = "0.1.0"
description = "WalkPick 地點搜尋服務"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.uv]
package = false
